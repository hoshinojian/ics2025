#ifndef __TRM_H__
#define __TRM_H__

#include <am.h>

// ==============================================================================
// 1. 全局变量声明 (Global Variables)
// ==============================================================================

// 声明 heap 变量，这样 lib.c 里的 malloc 才能通过 extern 引用它
// 注意：在 .h 文件里要用 extern，不能直接赋值
extern Area heap;

// ==============================================================================
// 2. 函数声明 (Function Prototypes)
// ==============================================================================

// 打印字符 (TRM 提供的最基础输出功能)
void putch(char ch);

// 停机 (TRM 提供的最基础结束功能)
void halt(int code);

// 初始化 TRM (通常由汇编启动代码 start.S 调用)
void _trm_init();

#endif